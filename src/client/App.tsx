import "./App.css";
import { useState } from "react";
import Box from '@mui/material/Box'

function App() {
  const [count, setCount] = useState(0);

  return (
    <Box>
      Scroll Rack
    </Box>
  );
}

export default App;
